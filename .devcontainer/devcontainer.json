{
  "name": "DeepCode Python 3.10",
  "image": "mcr.microsoft.com/devcontainers/python:3.10",
  "postCreateCommand": "python -m pip install --upgrade pip setuptools wheel && pip install -r requirements.txt"
}